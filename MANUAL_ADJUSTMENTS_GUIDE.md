# دليل التعديلات اليدوية لمواقيت الصلاة

## 📋 ما الجديد؟

تم إضافة نظام **Offsets** (الفروقات الزمنية) الذي يسمح بالتعديل اليدوي على مواقيت الصلاة مع الحفاظ على التعديلات حتى بعد:
- تحديث الصفحة
- التحديث اليومي التلقائي للمواقيت
- تغيير المدينة أو الدولة

## 🔧 كيف يعمل النظام؟

### قبل التعديلات
- كان التطبيق يجلب مواقيت الصلاة من API
- عند التعديل اليدوي، يتم حفظ الأوقات الجديدة
- **لكن** عند تحديث الصفحة أو اليوم التالي، يتم استبدال التعديلات بأوقات API الجديدة ❌

### بعد التعديلات ✅
1. يجلب التطبيق مواقيت الصلاة من API (الأوقات الأساسية)
2. عند التعديل اليدوي، يحسب النظام **الفرق الزمني** بين الوقت الجديد والوقت من API
3. يحفظ هذا الفرق كـ **offset** (بالدقائق)
4. في كل مرة يتم جلب أوقات جديدة من API، يتم تطبيق الـ offset تلقائياً
5. النتيجة: التعديلات اليدوية تبقى دائماً! 🎯

## 📝 مثال عملي

### السيناريو
- وقت صلاة الفجر من API: `04:02`
- تريد تعديله إلى: `04:07` (تأخير 5 دقائق)

### ما يحدث في الخلفية
1. النظام يحسب الفرق: `04:07 - 04:02 = +5 دقائق`
2. يحفظ الـ offset: `fajr_offset = +5`
3. يحفظ الوقت المعدّل: `04:07`

### اليوم التالي
- API يعطي وقت جديد للفجر: `04:00`
- النظام يطبق الـ offset تلقائياً: `04:00 + 5 = 04:05`
- النتيجة المعروضة: `04:05` ✨

## 🎛️ كيفية الاستخدام

1. افتح الإعدادات (⚙️)
2. اذهب إلى قسم **"أوقات الصلاة (يدوي)"**
3. عدّل الوقت المطلوب
4. اضغط **"حفظ وإغلاق"**
5. التعديل سيبقى دائماً! ✅

## 🔍 التفاصيل التقنية

### البيانات المحفوظة في localStorage

#### `alMoazinClockSettings`
يحتوي على:
```json
{
  "prayerTimes": { "fajr": "04:07", ... },
  "prayerTimeOffsets": { "fajr": 5, "dhuhr": 0, ... },
  "shuruqTime": "05:40",
  "shuruqOffset": 7,
  "jumuahTime": "12:43",
  "jumuahOffset": 0
}
```

#### `alMoazinPrayerTimes`
يحتوي على:
```json
{
  "date": "2025-10-08",
  "times": { "fajr": "04:02", ... },  // الأوقات الأصلية من API
  "shuruq": "05:33",
  "adjustedTimes": { "fajr": "04:07", ... },  // الأوقات بعد تطبيق offsets
  "adjustedShuruq": "05:40"
}
```

### الملفات المعدّلة

1. **AlMoazinClock.tsx**
   - إضافة `prayerTimeOffsets`, `shuruqOffset`, `jumuahOffset` للـ ClockSettings
   - إضافة دالة `applyOffsetToTime()` لتطبيق الفروقات الزمنية
   - تعديل `fetchPrayerTimesAndWeather()` لتطبيق offsets على API data

2. **SettingsPanel.tsx**
   - إضافة دالة `calculateTimeDifference()` لحساب الفرق بين وقتين
   - تعديل `handlePrayerTimeChange()` لحساب وحفظ offsets تلقائياً
   - حفظ الأوقات الأصلية من API عند فتح الإعدادات

## ✨ الميزات

- ✅ التعديلات اليدوية تبقى دائماً
- ✅ تحديث تلقائي يومي مع الحفاظ على التعديلات
- ✅ دعم التقديم والتأخير (قيم موجبة وسالبة)
- ✅ دعم جميع الصلوات (الفجر، الظهر، العصر، المغرب، العشاء)
- ✅ دعم الشروق والجمعة أيضاً
- ✅ سهولة الاستخدام - لا حاجة لإدخال الـ offset يدوياً

## 🧪 الاختبار

لاختبار أن النظام يعمل بشكل صحيح:

1. عدّل وقت صلاة (مثلاً الفجر)
2. احفظ الإعدادات
3. حدّث الصفحة (F5)
4. تأكد أن الوقت المعدّل لا يزال موجوداً ✅
5. اضغط زر التحديث (🔄) لجلب أوقات جديدة من API
6. تأكد أن التعديل تم تطبيقه على الأوقات الجديدة ✅

## 📞 الدعم

تم تطويره بواسطة: **ناصر عوض للأجهزة الصوتية والإلكترونيات**
الهاتف: 0795644423

---

**الإصدار:** 1.4.0
**تاريخ التحديث:** 2025-10-08
